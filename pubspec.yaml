name: comptaflow
description: Comptaflow - mobile business companion for Moroccan auto-entrepreneurs.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  provider: ^6.1.5+1
  supabase_flutter: ^2.10.3
  shared_preferences: ^2.5.3
  pdf: ^3.11.3
  printing: ^5.14.2
  flutter_local_notifications: ^19.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
